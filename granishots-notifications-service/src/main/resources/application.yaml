spring:
  application:
    name: granishots-catalog-service

  datasource:
    url: ${SPRING_DATASOURCE_URL}
    username: ${SPRING_DATASOURCE_USERNAME}
    password: ${SPRING_DATASOURCE_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: ${PORT}

# ── Twilio Configuration ──────────────────────────────────────────────────────
twilio:
  account-sid: AC823044972c85ed8920b1347c4e1c7fd9
  auth-token: 24f1b4c2699f11916d4ecd779a7c63d5
  phone-number: "+18392227142"

# ── Email Configuration (SendGrid — pendiente activación) ─────────────────────
sendgrid:
  api-key: "PENDIENTE"
  from-email: "jr3986259@gmail.com"
  from-name: "GraniShots"
  enabled: false
