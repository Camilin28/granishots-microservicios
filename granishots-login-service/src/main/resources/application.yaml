spring:
  application:
    name: granishots-login-service
  datasource:
    url: jdbc:postgresql://localhost:5432/login_db
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

server:
  port: 8086
